name: libmonet
description: Dynamic, user-friendly, design themes.
version: 1.0.0
homepage:

environment:
  sdk: '>=3.1.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  matcher: ^0.12.16

dev_dependencies:
  test: ^1.24.8
  flutter_lints: ^2.0.0
  material_color_utilities: ^0.5.0

flutter: