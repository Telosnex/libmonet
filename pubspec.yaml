name: libmonet
description: Dynamic, user-friendly, design themes.
version: 1.0.0
homepage:

environment:
  sdk: '>=3.1.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0
  json_serializable: ^6.7.1
  matcher: ^0.12.16
  squadron: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  material_color_utilities: ^0.11.1
  build_runner:
  squadron_builder:
    git:
      url: https://github.com/telosnex/squadron_builder
      ref: main

flutter: